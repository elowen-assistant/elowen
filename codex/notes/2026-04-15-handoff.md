# Elowen Handoff - 2026-04-15

## Current State

- Slice set `0` through `32` is complete.
- `Slice 30 - UI Browser Automation` is closed out on merged `main`.
- `Slice 31 - Chat Surface And Draft UX Polish` is closed out on merged `main` in `elowen-api`, `elowen-ui`, and `elowen-workspace`.
- `Slice 32 - Identity And Authorization Hardening` is closed out on merged `main` in `elowen-api`, `elowen-ui`, `elowen-platform`, and `elowen-workspace`.
- The next planned slice is `Slice 33 - Repository Policy And Selection UX`.

## Slice 32 Progress Summary

- `elowen-api` slice work now includes:
  - local-account auth from `ELOWEN_UI_AUTH_CONFIG_PATH` with Argon2id password verification
  - legacy shared-password compatibility fallback that synthesizes a single admin identity when no account config is supplied
  - role-aware session persistence with `account_username`, `display_name`, `role`, and `last_seen_at`
  - per-route `viewer`, `operator`, and `admin` authorization instead of the prior binary session wall
  - server-derived approval and note-promotion actor identity
- `elowen-ui` slice work now includes:
  - local-account username/password sign-in with legacy shared-password fallback support
  - actor and role display in the authenticated shell
  - read-only viewer gating plus operator/admin affordance gating for composer, dispatch, note promotion, and approvals
  - updated Playwright harness coverage for admin, operator, and viewer sessions
- `elowen-platform` slice work now includes:
  - `ELOWEN_UI_AUTH_CONFIG_PATH` and `ELOWEN_UI_COOKIE_SECURE` wiring in compose env
  - local and VPS env template updates for the new auth settings
  - `env/ui-auth.example.toml` as the local-account config shape reference

## Slice 31 Closeout Summary

- `elowen-api` merged Slice 31 work includes:
  - cleaner execution-draft refinement synthesis for repository, base branch, title, and request text
  - summary-first lifecycle replies with operational detail retained in `job_result.details`
  - reduced duplicate draft-handoff copy in conversational replies
- `elowen-ui` merged Slice 31 work includes:
  - stronger chat transcript differentiation between activity messages and final-result surfaces
  - visible execution-draft handoff cards with dispatch actions
  - local timestamp formatting plus `Ctrl+Enter` and `Cmd+Enter` composer submission
  - new Playwright coverage for draft visibility, collapsed operational details, and keyboard submit
- Production is still deployed from merged `elowen-ui` `main` using:
  - `ghcr.io/elowen-assistant/elowen-ui:sha-bf1baf317d02a0c16936aacc13f2e849cab1f273`

## Validation Summary

- Automated validation passed during Slice 32 implementation:
  - `elowen-api`: `cargo check`, `cargo fmt --check`, `cargo test --quiet`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`
  - `elowen-ui`: `cargo check`, `cargo fmt --check`, `cargo test --quiet`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`, `npm run test:e2e`
- Live deployment verification on 2026-04-20:
  - environment: `https://api.ericburden.dev/` redeployed onto local `elowen-api-local:slice32-viewer-uat` and `elowen-ui-local:slice32-viewer-uat` images with VPS account-auth config enabled
  - auth session reports local-account mode before sign-in: pass
    notes: `GET /api/v1/auth/session` returned `auth_mode: "local_accounts"` with no authenticated actor
  - viewer account login succeeds against the live API: pass
    notes: `POST /api/v1/auth/login` with the Viewer account returned an authenticated session payload with `permissions: ["view"]`
- Automated validation passed during Slice 31 implementation:
  - `elowen-api`: `cargo check`, `cargo test --quiet`
  - `elowen-api`: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --quiet`
  - `elowen-ui`: `cargo check`, `cargo test --quiet`, `npm run test:e2e`
  - `elowen-ui`: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --quiet`, `npm run test:e2e`
- Manual UAT passed on 2026-04-15 against the local authenticated UI flow:
  - environment: local Slice 31 branches with the Playwright-backed browser harness and authenticated chat shell
  - draft-ready conversational reply shows a visible execution-draft handoff: pass
    notes: draft card displayed title, repository, base branch, rationale, and dispatch request text
  - operational detail is hidden by default and readable when opened: pass
    notes: result summary remained primary and `Operational Details` exposed the detailed execution text on demand
  - dispatch-ready draft still promotes into a normal job: pass
    notes: dispatch returned a normal job payload and the UI surfaced the promotion status for `job-031d`
  - composer submit works with `Ctrl+Enter` and returns a new draft response: pass
    notes: keyboard submit sent the chat message and the returned assistant reply rendered a new execution-draft card
  - timestamps render in local operator format and the pinned composer remains anchored while the message pane scrolls: pass
    notes: timestamps rendered as local-readable labels and the sticky composer stayed anchored during transcript interaction
  - metadata-only refinement does not pollute the draft request body: pass with automation evidence
    notes: covered by the new targeted API unit test because the browser harness is static and does not exercise live orchestrator-side draft refinement
  - known gaps: none

## Latest Known-Good Revisions

- `elowen-ui` merged `main`: `411d48c`
- `elowen-api` merged `main`: `640d8bf`
- `elowen-edge`: `de206a0`
- `elowen-notes`: `102630e`
- `elowen-platform`: `8b90c55`

## Next Resume Focus

1. Start `Slice 33 - Repository Policy And Selection UX`.
2. Run the post-merge browser walkthrough against the live account-auth deployment:
   verify `viewer`, `operator`, and `admin` affordance gating plus approval and note-promotion audit identity from the real UI shell
3. Keep production sourcing from merged `main` branches and continue publishing deliberate image tags after future slice closeout
