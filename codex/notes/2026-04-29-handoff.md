# 2026-04-29 Handoff

## Slice Status

- `Slice 44 - Secrets And Key Material Hardening` is complete locally and merged to child `main` branches.
- Workspace branch: `slice/44-secrets-and-key-material-hardening`.
- Next planned slice: `Slice 45 - Admin-Driven Edge Enrollment Bootstrap`.

## Child Repo Closeout Commits

- `elowen-edge`: `9082efb` - `Complete Slice 44 edge secret hardening`
- `elowen-api`: `214507a` - `Complete Slice 44 signer secret loading`
- `elowen-platform`: `d4a8400` - `Complete Slice 44 platform secret docs`

## Delivered

- Edge trust private keys can be loaded through provider-backed TOML references:
  - `[trust.edge_signing_key]`
  - `[trust.previous_edge_signing_key]`
- Legacy `edge_signing_key_path` fields remain supported as compatibility aliases.
- Windows edge private key material can be imported into a DPAPI-protected local file with `elowen-edge trust import-key --from PATH --to PATH --provider dpapi`.
- Linux/container file-provider secrets still enforce narrow permissions unless the explicit dev/test override is set.
- The TUI reports edge key backend state and previous-key state without exposing private material; First Run uses compact text while Diagnostics keeps detailed backend/path information.
- The API can load orchestrator signer keys from mounted files via `ELOWEN_ORCHESTRATOR_SIGNING_KEY_FILE` and `ELOWEN_ORCHESTRATOR_SIGNING_KEY_FILES`.
- Signer lifecycle state continues to persist only public key ids, public keys, status, actor/reason, and timestamps.
- Compose, VPS, Kubernetes, and laptop-edge docs now distinguish private key material from public trust bundles and show hardened mounting/storage patterns.

## Validation

Automated validation passed after feature implementation and again after the required refactor pass:

- `elowen-edge`
  - `cargo fmt --check`
  - `cargo check`
  - `cargo test --quiet`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo doc --no-deps`
- `elowen-api`
  - `cargo fmt --check`
  - `cargo check`
  - `cargo test --quiet`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo doc --no-deps`
- `elowen-platform`
  - `docker compose -f compose/docker-compose.vps.yml config`

Manual UAT passed:

- Windows DPAPI import created a protected local file.
- Windows edge registered successfully with DPAPI provider.
- Windows file-provider compatibility registration still worked.
- Linux/container file provider rejected broad-readable key material and succeeded after `chmod 600`.
- Orchestrator signer file loading, signer activation, signer retirement, registration challenge export, stale trust-bundle rejection, rotation, rotation confirmation, revocation, and fresh-key recovery all passed.
- Installed local Windows edge was migrated to DPAPI and inspected interactively in the TUI.

Known gap:

- Linux/VPS host-level systemd service UAT still requires a target VPS host. Slice 44 does not claim fresh host-level systemd validation beyond docs and container/file-provider validation.

## Resume

- Push child `main` branches and the workspace closeout branch when ready.
- Start `Slice 45 - Admin-Driven Edge Enrollment Bootstrap`.
